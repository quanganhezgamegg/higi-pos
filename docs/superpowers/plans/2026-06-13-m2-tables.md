# M2 — Bàn & sơ đồ — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho phép quản lý khu vực và bàn (thêm/sửa/xoá/bật-tắt), hiển thị sơ đồ bàn `/tables` theo khu vực (tab), bàn dạng card màu trạng thái (trống / ngừng dùng). Trạng thái "đang phục vụ" (bàn có đơn OPEN) sẽ được thêm ở M3 — M2 chỉ biết hai trạng thái từ `is_active`.

**Architecture:** Tiếp nối M1 — Rust sở hữu SQLite, expose Tauri commands (`commands/tables.rs` → `repo/tables.rs` → SQLite); React gọi qua `lib/api/tables.ts`, màn hình `/tables` dùng shadcn tabs + card grid. Không có nghiệp vụ money trong M2.

**Tech Stack:** rusqlite + rusqlite_migration (sẵn), React + shadcn/ui, cargo test, `npm run build`.

**Spec:** `docs/superpowers/specs/2026-06-12-higi-pos-design.md` §sơ đồ bàn.
**Contract (binding):** `docs/superpowers/plans/2026-06-13-m2-m7-contract.md` §1 (SQL), §3 (commands + domain), §5 (conventions).
**Mẫu code tham chiếu:** `src-tauri/src/repo/menu.rs`, `src-tauri/src/commands/menu.rs`, `src-tauri/src/db/migrations.rs`, `src/lib/api/menu.ts`.

> **M2 scope boundary:** `list_table_status` trong M2 trả tất cả bàn với `status = "TRONG"` (không có orders). M3 sẽ mở rộng query thành LEFT JOIN orders để suy ra `"DANG_PHUC_VU"` — đây là forward extension đã ghi nhận, không cần schema thay đổi. Chuyển bàn / gộp bàn KHÔNG triển khai trong M2 (cần orders → M3).

---

## File Structure

| File | Trách nhiệm |
|---|---|
| `src-tauri/migrations/0003_tables.sql` | Tạo bảng `areas`, `tables` + index |
| `src-tauri/src/domain/tables.rs` | Struct serde: `Area`, `Table`, `TableInput`, `TableStatus`, `TableState` |
| Sửa `src-tauri/src/domain/mod.rs` | Thêm `pub mod tables;` |
| `src-tauri/src/repo/tables.rs` | CRUD areas + tables, guard xoá khu vực còn bàn, `list_table_status` (M2: trả TRONG hết), unit tests in-memory |
| Sửa `src-tauri/src/repo/mod.rs` | Thêm `pub mod tables;` |
| `src-tauri/src/commands/tables.rs` | Tauri commands gọi repo; `lock` helper như M1; map lỗi → String |
| Sửa `src-tauri/src/commands/mod.rs` | Thêm `pub mod tables;` |
| Sửa `src-tauri/src/db/migrations.rs` | Thêm `M::up(include_str!("../../migrations/0003_tables.sql"))` + test schema |
| Sửa `src-tauri/src/lib.rs` | Thêm commands tables vào `generate_handler!` |
| `src/lib/api/tables.ts` | Wrapper `invoke` + types TS khớp domain |
| `src/routes/Tables.tsx` | Khung `/tables` — tabs khu vực, card bàn |
| `src/routes/tables/AreaManager.tsx` | Dialog quản lý khu vực (thêm/sửa/xoá) |
| `src/routes/tables/TableCard.tsx` | Card bàn với màu trạng thái + menu sửa/xoá |
| `src/routes/tables/TableForm.tsx` | Form thêm/sửa bàn (tên, khu vực, seats, sort_order) |
| Sửa `src/App.tsx`, `src/routes/Home.tsx` | Đăng ký route `/tables` + link từ Home |

---

## Task 1: Migration 0003 — bảng areas + tables

**Files:** Tạo `src-tauri/migrations/0003_tables.sql`; Sửa `src-tauri/src/db/migrations.rs`.

- [ ] **Step 1: Tạo `src-tauri/migrations/0003_tables.sql`** — SQL lấy NGUYÊN VĂN từ contract §1 M2:

```sql
CREATE TABLE areas (
  id         INTEGER PRIMARY KEY,
  name       TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_active  INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE tables (
  id         INTEGER PRIMARY KEY,
  area_id    INTEGER NOT NULL REFERENCES areas(id),
  name       TEXT NOT NULL,
  seats      INTEGER,
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_active  INTEGER NOT NULL DEFAULT 1
);
CREATE INDEX idx_tables_area ON tables(area_id);
```

- [ ] **Step 2: Đăng ký migration** — trong `src-tauri/src/db/migrations.rs`, thêm dòng mới ở CUỐI vec (giữ nguyên 0001 và 0002):

```rust
pub fn migrations() -> Migrations<'static> {
    Migrations::new(vec![
        M::up(include_str!("../../migrations/0001_init.sql")),
        M::up(include_str!("../../migrations/0002_menu.sql")),
        M::up(include_str!("../../migrations/0003_tables.sql")),
    ])
}
```

- [ ] **Step 3: Thêm test schema** — thêm test sau `migration_creates_menu_tables` trong module `tests` của `migrations.rs`:

```rust
#[test]
fn migration_creates_tables_tables() {
    let mut c = rusqlite::Connection::open_in_memory().unwrap();
    run(&mut c).unwrap();
    for t in ["areas", "tables"] {
        let n: i64 = c
            .query_row(
                "SELECT count(*) FROM sqlite_master WHERE type='table' AND name=?1",
                [t],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(n, 1, "missing table {t}");
    }
    // index phải tồn tại
    let idx: i64 = c
        .query_row(
            "SELECT count(*) FROM sqlite_master WHERE type='index' AND name='idx_tables_area'",
            [],
            |r| r.get(0),
        )
        .unwrap();
    assert_eq!(idx, 1, "missing index idx_tables_area");
}
```

- [ ] **Step 4: Chạy test** — Run: `cargo test --manifest-path src-tauri/Cargo.toml migration`
Expected: `migrations_are_valid`, `migration_creates_menu_tables`, `migration_creates_tables_tables` đều `ok`.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/migrations/0003_tables.sql src-tauri/src/db/migrations.rs
git commit -m "feat(db): add areas and tables migration (0003)"
```

---

## Task 2: Domain structs

**Files:** Tạo `src-tauri/src/domain/tables.rs`; Sửa `src-tauri/src/domain/mod.rs`.

- [ ] **Step 1: Thêm module** — trong `src-tauri/src/domain/mod.rs`, thêm dòng:

```rust
pub mod tables;
```

(giữ `pub mod menu;` hiện có)

- [ ] **Step 2: Tạo `src-tauri/src/domain/tables.rs`** — structs/enums khớp NGUYÊN VĂN contract §3:

```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize)]
pub struct Area {
    pub id: i64,
    pub name: String,
    pub sort_order: i64,
    pub is_active: bool,
}

#[derive(Debug, Clone, Serialize)]
pub struct Table {
    pub id: i64,
    pub area_id: i64,
    pub name: String,
    pub seats: Option<i64>,
    pub sort_order: i64,
    pub is_active: bool,
}

#[derive(Debug, Clone, Deserialize)]
pub struct TableInput {
    pub area_id: i64,
    pub name: String,
    pub seats: Option<i64>,
    pub sort_order: i64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "SCREAMING_SNAKE_CASE")]
pub enum TableState {
    Trong,
    DangPhucVu,
}

#[derive(Debug, Clone, Serialize)]
pub struct TableStatus {
    pub table: Table,
    pub status: TableState,
    pub open_order_id: Option<i64>,
}
```

- [ ] **Step 3: Build** — Run: `cargo build --manifest-path src-tauri/Cargo.toml`
Expected: biên dịch OK.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/domain/tables.rs src-tauri/src/domain/mod.rs
git commit -m "feat(domain): add tables domain structs (Area, Table, TableInput, TableStatus)"
```

---

## Task 3: Repo — areas CRUD (TDD)

**Files:** Tạo `src-tauri/src/repo/tables.rs`; Sửa `src-tauri/src/repo/mod.rs`.

Theo đúng mẫu `repo/menu.rs`: hàm nhận `&Connection`, trả `rusqlite::Result<T>`, validate tên, reload-and-return sau write, helper `row_to_area`.

- [ ] **Step 1: Khai báo module** — thêm `pub mod tables;` vào `src-tauri/src/repo/mod.rs`.

- [ ] **Step 2: Viết test THẤT BẠI trước** — tạo `src-tauri/src/repo/tables.rs` chỉ với phần test areas:

```rust
use crate::domain::tables::*;
use rusqlite::{params, Connection};

#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::migrations;

    fn conn() -> Connection {
        let mut c = Connection::open_in_memory().unwrap();
        migrations::run(&mut c).unwrap();
        c
    }

    #[test]
    fn area_create_list_update() {
        let c = conn();
        let a = create_area(&c, "Tầng 1", 0).unwrap();
        assert_eq!(a.name, "Tầng 1");
        assert!(a.is_active);

        let all = list_areas(&c, true).unwrap();
        assert_eq!(all.len(), 1);

        update_area(&c, a.id, "Sảnh", 5, false).unwrap();
        let one = list_areas(&c, true).unwrap().pop().unwrap();
        assert_eq!(one.name, "Sảnh");
        assert_eq!(one.sort_order, 5);
        assert!(!one.is_active);

        // include_inactive=false ẩn khu đã tắt
        assert_eq!(list_areas(&c, false).unwrap().len(), 0);
    }

    #[test]
    fn area_create_rejects_blank_name() {
        let c = conn();
        assert!(create_area(&c, "  ", 0).is_err());
    }
}
```

- [ ] **Step 3: Chạy test → FAIL** — Run: `cargo test --manifest-path src-tauri/Cargo.toml area_create`
Expected: lỗi biên dịch "cannot find function `create_area`/`list_areas`/`update_area`".

- [ ] **Step 4: Viết impl** — thêm vào ĐẦU `repo/tables.rs` (trên `#[cfg(test)]`):

```rust
fn row_to_area(row: &rusqlite::Row) -> rusqlite::Result<Area> {
    Ok(Area {
        id: row.get("id")?,
        name: row.get("name")?,
        sort_order: row.get("sort_order")?,
        is_active: row.get::<_, i64>("is_active")? != 0,
    })
}

pub fn create_area(conn: &Connection, name: &str, sort_order: i64) -> rusqlite::Result<Area> {
    let name = name.trim();
    if name.is_empty() {
        return Err(rusqlite::Error::InvalidParameterName("name rỗng".into()));
    }
    conn.execute(
        "INSERT INTO areas(name, sort_order) VALUES (?1, ?2)",
        params![name, sort_order],
    )?;
    let id = conn.last_insert_rowid();
    conn.query_row("SELECT * FROM areas WHERE id=?1", [id], row_to_area)
}

pub fn list_areas(conn: &Connection, include_inactive: bool) -> rusqlite::Result<Vec<Area>> {
    let sql = if include_inactive {
        "SELECT * FROM areas ORDER BY sort_order, id"
    } else {
        "SELECT * FROM areas WHERE is_active=1 ORDER BY sort_order, id"
    };
    let mut stmt = conn.prepare(sql)?;
    stmt.query_map([], row_to_area)?.collect()
}

pub fn update_area(
    conn: &Connection,
    id: i64,
    name: &str,
    sort_order: i64,
    is_active: bool,
) -> rusqlite::Result<()> {
    let name = name.trim();
    if name.is_empty() {
        return Err(rusqlite::Error::InvalidParameterName("name rỗng".into()));
    }
    conn.execute(
        "UPDATE areas SET name=?2, sort_order=?3, is_active=?4 WHERE id=?1",
        params![id, name, sort_order, is_active as i64],
    )?;
    Ok(())
}

/// Xoá khu vực — bị chặn nếu còn bàn (mirror M1 delete_category guard).
pub fn delete_area(conn: &Connection, id: i64) -> rusqlite::Result<()> {
    let n: i64 = conn.query_row(
        "SELECT count(*) FROM tables WHERE area_id=?1",
        [id],
        |r| r.get(0),
    )?;
    if n > 0 {
        return Err(rusqlite::Error::InvalidParameterName(
            "Khu vực còn bàn — hãy xoá bàn trước hoặc vô hiệu hoá khu vực".into(),
        ));
    }
    conn.execute("DELETE FROM areas WHERE id=?1", [id])?;
    Ok(())
}
```

- [ ] **Step 5: Chạy test → PASS** — Run: `cargo test --manifest-path src-tauri/Cargo.toml area_create`
Expected: 2 test pass.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/repo/tables.rs src-tauri/src/repo/mod.rs
git commit -m "feat(repo): add area CRUD with tests (tables.rs)"
```

---

## Task 4: Repo — tables CRUD (TDD)

**Files:** Sửa `src-tauri/src/repo/tables.rs`.

- [ ] **Step 1: Thêm test THẤT BẠI** vào module `tests` trong `repo/tables.rs`:

```rust
    fn seed_area(c: &Connection) -> Area {
        create_area(c, "Tầng 1", 0).unwrap()
    }

    #[test]
    fn table_create_list_update() {
        let c = conn();
        let area = seed_area(&c);
        let t = create_table(&c, TableInput {
            area_id: area.id, name: "B01".into(), seats: Some(4), sort_order: 0,
        }).unwrap();
        assert_eq!(t.name, "B01");
        assert_eq!(t.seats, Some(4));
        assert!(t.is_active);

        let all = list_tables(&c, Some(area.id), true).unwrap();
        assert_eq!(all.len(), 1);

        let updated = update_table(&c, t.id, TableInput {
            area_id: area.id, name: "B01-VIP".into(), seats: Some(6), sort_order: 1,
        }).unwrap();
        assert_eq!(updated.name, "B01-VIP");
        assert_eq!(updated.seats, Some(6));
    }

    #[test]
    fn table_set_active_and_delete() {
        let c = conn();
        let area = seed_area(&c);
        let t = create_table(&c, TableInput {
            area_id: area.id, name: "B02".into(), seats: None, sort_order: 0,
        }).unwrap();
        set_table_active(&c, t.id, false).unwrap();
        assert_eq!(list_tables(&c, None, false).unwrap().len(), 0);
        assert_eq!(list_tables(&c, None, true).unwrap().len(), 1);
        delete_table(&c, t.id).unwrap();
        assert_eq!(list_tables(&c, None, true).unwrap().len(), 0);
    }

    #[test]
    fn delete_area_blocked_when_has_tables() {
        let c = conn();
        let area = seed_area(&c);
        create_table(&c, TableInput {
            area_id: area.id, name: "B03".into(), seats: None, sort_order: 0,
        }).unwrap();
        assert!(delete_area(&c, area.id).is_err());
    }

    #[test]
    fn table_create_rejects_blank_name() {
        let c = conn();
        let area = seed_area(&c);
        assert!(create_table(&c, TableInput {
            area_id: area.id, name: "  ".into(), seats: None, sort_order: 0,
        }).is_err());
    }

    #[test]
    fn list_table_status_returns_trong_in_m2() {
        let c = conn();
        let area = seed_area(&c);
        create_table(&c, TableInput {
            area_id: area.id, name: "B04".into(), seats: None, sort_order: 0,
        }).unwrap();
        let statuses = list_table_status(&c).unwrap();
        assert_eq!(statuses.len(), 1);
        assert!(matches!(statuses[0].status, TableState::Trong));
        assert!(statuses[0].open_order_id.is_none());
    }
```

- [ ] **Step 2: Chạy test → FAIL** — Run: `cargo test --manifest-path src-tauri/Cargo.toml table_`
Expected: lỗi biên dịch "cannot find function `create_table`/`update_table`/`set_table_active`/`delete_table`/`list_table_status`".

- [ ] **Step 3: Viết impl** — thêm vào phần impl (trên `#[cfg(test)]`) trong `repo/tables.rs`:

```rust
fn row_to_table(row: &rusqlite::Row) -> rusqlite::Result<Table> {
    Ok(Table {
        id: row.get("id")?,
        area_id: row.get("area_id")?,
        name: row.get("name")?,
        seats: row.get("seats")?,
        sort_order: row.get("sort_order")?,
        is_active: row.get::<_, i64>("is_active")? != 0,
    })
}

fn load_table(conn: &Connection, id: i64) -> rusqlite::Result<Table> {
    conn.query_row("SELECT * FROM tables WHERE id=?1", [id], row_to_table)
}

pub fn create_table(conn: &Connection, input: TableInput) -> rusqlite::Result<Table> {
    let name = input.name.trim();
    if name.is_empty() {
        return Err(rusqlite::Error::InvalidParameterName("name rỗng".into()));
    }
    // kiểm tra area tồn tại
    let area_exists: i64 = conn.query_row(
        "SELECT count(*) FROM areas WHERE id=?1",
        [input.area_id],
        |r| r.get(0),
    )?;
    if area_exists == 0 {
        return Err(rusqlite::Error::InvalidParameterName("area_id không tồn tại".into()));
    }
    conn.execute(
        "INSERT INTO tables(area_id, name, seats, sort_order) VALUES (?1, ?2, ?3, ?4)",
        params![input.area_id, name, input.seats, input.sort_order],
    )?;
    let id = conn.last_insert_rowid();
    load_table(conn, id)
}

pub fn list_tables(
    conn: &Connection,
    area_id: Option<i64>,
    include_inactive: bool,
) -> rusqlite::Result<Vec<Table>> {
    let mut sql = String::from("SELECT * FROM tables WHERE 1=1");
    if !include_inactive {
        sql.push_str(" AND is_active=1");
    }
    if area_id.is_some() {
        sql.push_str(" AND area_id=?1");
    }
    sql.push_str(" ORDER BY sort_order, id");
    let mut stmt = conn.prepare(&sql)?;
    if let Some(aid) = area_id {
        stmt.query_map([aid], row_to_table)?.collect()
    } else {
        stmt.query_map([], row_to_table)?.collect()
    }
}

pub fn update_table(conn: &Connection, id: i64, input: TableInput) -> rusqlite::Result<Table> {
    let name = input.name.trim();
    if name.is_empty() {
        return Err(rusqlite::Error::InvalidParameterName("name rỗng".into()));
    }
    conn.execute(
        "UPDATE tables SET area_id=?2, name=?3, seats=?4, sort_order=?5 WHERE id=?1",
        params![id, input.area_id, name, input.seats, input.sort_order],
    )?;
    load_table(conn, id)
}

pub fn set_table_active(conn: &Connection, id: i64, is_active: bool) -> rusqlite::Result<()> {
    conn.execute(
        "UPDATE tables SET is_active=?2 WHERE id=?1",
        params![id, is_active as i64],
    )?;
    Ok(())
}

pub fn delete_table(conn: &Connection, id: i64) -> rusqlite::Result<()> {
    conn.execute("DELETE FROM tables WHERE id=?1", [id])?;
    Ok(())
}

/// M2: trả tất cả bàn với status=TRONG và open_order_id=None.
/// M3 sẽ thay thế body này bằng LEFT JOIN orders WHERE status='OPEN'.
pub fn list_table_status(conn: &Connection) -> rusqlite::Result<Vec<TableStatus>> {
    let tables = list_tables(conn, None, true)?;
    Ok(tables
        .into_iter()
        .map(|t| TableStatus {
            table: t,
            status: TableState::Trong,
            open_order_id: None,
        })
        .collect())
}
```

- [ ] **Step 4: Chạy test → PASS** — Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: tất cả test tables + areas + menu + migrations pass.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/repo/tables.rs
git commit -m "feat(repo): add table CRUD, area delete guard, list_table_status (M2 stub)"
```

---

## Task 5: Tauri commands

**Files:** Tạo `src-tauri/src/commands/tables.rs`; Sửa `src-tauri/src/commands/mod.rs`, `src-tauri/src/lib.rs`.

Theo đúng mẫu `commands/menu.rs`: `fn lock(...)`, `#[tauri::command]`, gọi repo, `.map_err(|e| e.to_string())`.

- [ ] **Step 1: Tạo `src-tauri/src/commands/tables.rs`**

```rust
use tauri::State;

use crate::db::AppDb;
use crate::domain::tables::{Area, Table, TableInput, TableStatus};
use crate::repo::tables;

fn lock<'a>(
    db: &'a State<'_, AppDb>,
) -> Result<std::sync::MutexGuard<'a, rusqlite::Connection>, String> {
    db.0.lock().map_err(|e| e.to_string())
}

#[tauri::command]
pub fn list_areas(db: State<AppDb>, include_inactive: bool) -> Result<Vec<Area>, String> {
    let conn = lock(&db)?;
    tables::list_areas(&conn, include_inactive).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn create_area(db: State<AppDb>, name: String, sort_order: i64) -> Result<Area, String> {
    let conn = lock(&db)?;
    tables::create_area(&conn, &name, sort_order).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn update_area(
    db: State<AppDb>,
    id: i64,
    name: String,
    sort_order: i64,
    is_active: bool,
) -> Result<(), String> {
    let conn = lock(&db)?;
    tables::update_area(&conn, id, &name, sort_order, is_active).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn delete_area(db: State<AppDb>, id: i64) -> Result<(), String> {
    let conn = lock(&db)?;
    tables::delete_area(&conn, id).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn list_tables(
    db: State<AppDb>,
    area_id: Option<i64>,
    include_inactive: bool,
) -> Result<Vec<Table>, String> {
    let conn = lock(&db)?;
    tables::list_tables(&conn, area_id, include_inactive).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn create_table(db: State<AppDb>, payload: TableInput) -> Result<Table, String> {
    let conn = lock(&db)?;
    tables::create_table(&conn, payload).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn update_table(db: State<AppDb>, id: i64, payload: TableInput) -> Result<Table, String> {
    let conn = lock(&db)?;
    tables::update_table(&conn, id, payload).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn set_table_active(db: State<AppDb>, id: i64, is_active: bool) -> Result<(), String> {
    let conn = lock(&db)?;
    tables::set_table_active(&conn, id, is_active).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn delete_table(db: State<AppDb>, id: i64) -> Result<(), String> {
    let conn = lock(&db)?;
    tables::delete_table(&conn, id).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn list_table_status(db: State<AppDb>) -> Result<Vec<TableStatus>, String> {
    let conn = lock(&db)?;
    tables::list_table_status(&conn).map_err(|e| e.to_string())
}
```

- [ ] **Step 2: Khai báo module** — thêm `pub mod tables;` vào `src-tauri/src/commands/mod.rs` (giữ `pub mod menu;`, `pub mod settings;`, `pub mod image;`).

- [ ] **Step 3: Đăng ký vào `invoke_handler`** — trong `src-tauri/src/lib.rs`, thêm 10 commands tables vào `tauri::generate_handler![...]` (giữ nguyên tất cả commands settings + menu + image):

```rust
.invoke_handler(tauri::generate_handler![
    // ... (settings, menu, image — giữ nguyên)
    commands::tables::list_areas,
    commands::tables::create_area,
    commands::tables::update_area,
    commands::tables::delete_area,
    commands::tables::list_tables,
    commands::tables::create_table,
    commands::tables::update_table,
    commands::tables::set_table_active,
    commands::tables::delete_table,
    commands::tables::list_table_status,
])
```

- [ ] **Step 4: Build + clippy** — Run: `cargo build --manifest-path src-tauri/Cargo.toml` rồi `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings`
Expected: biên dịch OK, clippy sạch.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/commands/tables.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat(commands): expose tables commands (area/table CRUD + list_table_status)"
```

---

## Task 6: Frontend API wrapper + types

**Files:** Tạo `src/lib/api/tables.ts`.

Theo mẫu `src/lib/api/menu.ts`: type TS khớp NGUYÊN VĂN domain Rust (snake_case field), invoke camelCase arg.

- [ ] **Step 1: Tạo `src/lib/api/tables.ts`**

```ts
import { invoke } from "@tauri-apps/api/core"

export type Area = {
  id: number
  name: string
  sort_order: number
  is_active: boolean
}

export type Table = {
  id: number
  area_id: number
  name: string
  seats: number | null
  sort_order: number
  is_active: boolean
}

export type TableInput = {
  area_id: number
  name: string
  seats: number | null
  sort_order: number
}

export type TableState = "TRONG" | "DANG_PHUC_VU"

export type TableStatus = {
  table: Table
  status: TableState
  open_order_id: number | null
}

// Areas
export const listAreas = (includeInactive = true) =>
  invoke<Area[]>("list_areas", { includeInactive })

export const createArea = (name: string, sortOrder = 0) =>
  invoke<Area>("create_area", { name, sortOrder })

export const updateArea = (id: number, name: string, sortOrder: number, isActive: boolean) =>
  invoke<void>("update_area", { id, name, sortOrder, isActive })

export const deleteArea = (id: number) => invoke<void>("delete_area", { id })

// Tables
export const listTables = (areaId: number | null = null, includeInactive = true) =>
  invoke<Table[]>("list_tables", { areaId, includeInactive })

export const createTable = (payload: TableInput) =>
  invoke<Table>("create_table", { payload })

export const updateTable = (id: number, payload: TableInput) =>
  invoke<Table>("update_table", { id, payload })

export const setTableActive = (id: number, isActive: boolean) =>
  invoke<void>("set_table_active", { id, isActive })

export const deleteTable = (id: number) => invoke<void>("delete_table", { id })

export const listTableStatus = () =>
  invoke<TableStatus[]>("list_table_status")
```

- [ ] **Step 2: Build typecheck** — Run: `npm run build`
Expected: tsc + vite OK.

- [ ] **Step 3: Commit**

```bash
git add src/lib/api/tables.ts
git commit -m "feat(api): add tables API wrapper and TS types"
```

---

## Task 7: Màn hình `/tables` — tab khu vực + card bàn

**Files:** Tạo `src/routes/Tables.tsx`, `src/routes/tables/AreaManager.tsx`, `src/routes/tables/TableCard.tsx`, `src/routes/tables/TableForm.tsx`; Sửa `src/App.tsx`, `src/routes/Home.tsx`.

Shadcn cần thêm: `card`, `badge` — cài nếu chưa có: `npx shadcn@latest add card badge`.

- [ ] **Step 1: Cài shadcn components** nếu chưa có — Run: `npx shadcn@latest add card badge`

- [ ] **Step 2: Tạo `src/routes/tables/TableCard.tsx`** — Card một bàn: hiển thị tên, số ghế (nếu có), badge trạng thái (trống = xanh lá, ngừng dùng = xám; đang phục vụ = cam, cho M3). Nút menu sửa/xoá/bật-tắt.

```tsx
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import type { Table, TableState } from "@/lib/api/tables"

type Props = {
  table: Table
  status: TableState
  onEdit: (t: Table) => void
  onDelete: (t: Table) => void
  onToggleActive: (t: Table) => void
}

function statusLabel(t: Table, status: TableState): { label: string; variant: "default" | "secondary" | "outline" } {
  if (!t.is_active) return { label: "Ngừng dùng", variant: "secondary" }
  if (status === "DANG_PHUC_VU") return { label: "Đang phục vụ", variant: "default" }
  return { label: "Trống", variant: "outline" }
}

export default function TableCard({ table, status, onEdit, onDelete, onToggleActive }: Props) {
  const { label, variant } = statusLabel(table, status)
  return (
    <Card className="min-h-[100px] min-w-[120px]">
      <CardContent className="flex flex-col items-center justify-between gap-2 p-4">
        <span className="text-lg font-semibold">{table.name}</span>
        {table.seats != null && (
          <span className="text-xs text-muted-foreground">{table.seats} ghế</span>
        )}
        <Badge variant={variant}>{label}</Badge>
        <div className="flex gap-1">
          <Button variant="ghost" size="sm" onClick={() => onEdit(table)}>Sửa</Button>
          <Button variant="ghost" size="sm" onClick={() => onToggleActive(table)}>
            {table.is_active ? "Tắt" : "Bật"}
          </Button>
          <Button variant="ghost" size="sm" onClick={() => onDelete(table)}>Xoá</Button>
        </div>
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 3: Tạo `src/routes/tables/TableForm.tsx`** — form thêm/sửa bàn (tên, area_id select, seats, sort_order). Khi lưu gọi `createTable`/`updateTable`. Async bọc try/catch.

```tsx
import { useState } from "react"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { createTable, updateTable, type Area, type Table, type TableInput } from "@/lib/api/tables"

type Props = {
  areas: Area[]
  editing?: Table | null
  defaultAreaId?: number
  onDone: () => void
}

export default function TableForm({ areas, editing, defaultAreaId, onDone }: Props) {
  const [name, setName] = useState(editing?.name ?? "")
  const [areaId, setAreaId] = useState(editing?.area_id ?? defaultAreaId ?? areas[0]?.id ?? 0)
  const [seats, setSeats] = useState<string>(editing?.seats?.toString() ?? "")
  const [sortOrder, setSortOrder] = useState(editing?.sort_order ?? 0)
  const [error, setError] = useState<string | null>(null)

  async function onSave() {
    setError(null)
    const payload: TableInput = {
      area_id: areaId,
      name: name.trim(),
      seats: seats.trim() === "" ? null : Number(seats),
      sort_order: sortOrder,
    }
    try {
      if (editing) await updateTable(editing.id, payload)
      else await createTable(payload)
      onDone()
    } catch (e) {
      setError(String(e))
    }
  }

  return (
    <div className="flex flex-col gap-3">
      <Input placeholder="Tên bàn (vd: B01)" value={name} onChange={(e) => setName(e.target.value)} />
      <select
        className="rounded-md border px-3 py-2"
        value={areaId}
        onChange={(e) => setAreaId(Number(e.target.value))}
      >
        {areas.map((a) => <option key={a.id} value={a.id}>{a.name}</option>)}
      </select>
      <Input
        type="number"
        placeholder="Số ghế (tuỳ chọn)"
        value={seats}
        onChange={(e) => setSeats(e.target.value)}
      />
      <Input
        type="number"
        placeholder="Thứ tự (sort_order)"
        value={sortOrder}
        onChange={(e) => setSortOrder(Number(e.target.value))}
      />
      {error && <p className="text-sm text-destructive">{error}</p>}
      <div className="flex justify-end gap-2">
        <Button variant="outline" onClick={onDone}>Huỷ</Button>
        <Button onClick={onSave}>Lưu</Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Tạo `src/routes/tables/AreaManager.tsx`** — Dialog quản lý khu vực: list areas (bật/tắt, xoá có xử lý lỗi), form thêm (tên). Theo khuôn `CategoryTab.tsx` trong M1.

```tsx
import { useEffect, useState } from "react"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Switch } from "@/components/ui/switch"
import {
  Dialog, DialogContent, DialogTitle, DialogTrigger,
} from "@/components/ui/dialog"
import {
  createArea, updateArea, deleteArea, listAreas, type Area,
} from "@/lib/api/tables"

type Props = { onChanged: () => void }

export default function AreaManager({ onChanged }: Props) {
  const [open, setOpen] = useState(false)
  const [areas, setAreas] = useState<Area[]>([])
  const [name, setName] = useState("")
  const [error, setError] = useState<string | null>(null)

  async function refresh() {
    try { setAreas(await listAreas(true)) } catch (e) { setError(String(e)) }
  }
  useEffect(() => { if (open) refresh() }, [open])

  async function onAdd() {
    setError(null)
    if (!name.trim()) return
    try { await createArea(name.trim(), areas.length); setName(""); await refresh(); onChanged() }
    catch (e) { setError(String(e)) }
  }
  async function onToggle(a: Area) {
    try { await updateArea(a.id, a.name, a.sort_order, !a.is_active); await refresh(); onChanged() }
    catch (e) { setError(String(e)) }
  }
  async function onDelete(a: Area) {
    setError(null)
    try { await deleteArea(a.id); await refresh(); onChanged() }
    catch (e) { setError(String(e)) }
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button variant="outline">Quản lý khu vực</Button>
      </DialogTrigger>
      <DialogContent>
        <DialogTitle>Khu vực</DialogTitle>
        <div className="flex gap-2">
          <Input placeholder="Tên khu vực" value={name} onChange={(e) => setName(e.target.value)} />
          <Button onClick={onAdd}>Thêm</Button>
        </div>
        {error && <p className="text-sm text-destructive">{error}</p>}
        <ul className="divide-y rounded-md border">
          {areas.map((a) => (
            <li key={a.id} className="flex items-center justify-between p-3">
              <span className={a.is_active ? "" : "text-muted-foreground line-through"}>{a.name}</span>
              <div className="flex items-center gap-3">
                <Switch checked={a.is_active} onCheckedChange={() => onToggle(a)} />
                <Button variant="ghost" size="sm" onClick={() => onDelete(a)}>Xoá</Button>
              </div>
            </li>
          ))}
        </ul>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 5: Tạo `src/routes/Tables.tsx`** — load `listAreas` (active only) + `listTableStatus`; tab mỗi khu vực = grid bàn; tab "Tất cả" luôn hiển thị đầu; nút "Thêm bàn" mở `Dialog` chứa `TableForm`; nút "Quản lý khu vực" mở `AreaManager`.

```tsx
import { useEffect, useState } from "react"
import { Link } from "react-router-dom"
import { Button } from "@/components/ui/button"
import { Dialog, DialogContent, DialogTitle, DialogTrigger } from "@/components/ui/dialog"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import {
  listAreas, listTableStatus, deleteTable, setTableActive,
  type Area, type Table, type TableStatus,
} from "@/lib/api/tables"
import TableCard from "@/routes/tables/TableCard"
import TableForm from "@/routes/tables/TableForm"
import AreaManager from "@/routes/tables/AreaManager"

export default function Tables() {
  const [areas, setAreas] = useState<Area[]>([])
  const [statuses, setStatuses] = useState<TableStatus[]>([])
  const [addOpen, setAddOpen] = useState(false)
  const [editing, setEditing] = useState<Table | null>(null)
  const [editOpen, setEditOpen] = useState(false)
  const [error, setError] = useState<string | null>(null)

  async function refresh() {
    try {
      setAreas(await listAreas(false))       // chỉ khu active cho tabs
      setStatuses(await listTableStatus())   // tất cả bàn active + trạng thái
    } catch (e) {
      setError(String(e))
    }
  }
  useEffect(() => { refresh() }, [])

  async function onDelete(t: Table) {
    setError(null)
    try { await deleteTable(t.id); await refresh() } catch (e) { setError(String(e)) }
  }
  async function onToggleActive(t: Table) {
    try { await setTableActive(t.id, !t.is_active); await refresh() } catch (e) { setError(String(e)) }
  }
  function onEdit(t: Table) { setEditing(t); setEditOpen(true) }

  function tablesForArea(areaId: number | null): TableStatus[] {
    if (areaId === null) return statuses
    return statuses.filter((s) => s.table.area_id === areaId)
  }

  return (
    <div className="mx-auto flex max-w-5xl flex-col gap-4 p-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">Sơ đồ bàn</h1>
        <div className="flex gap-2">
          <AreaManager onChanged={refresh} />
          <Dialog open={addOpen} onOpenChange={setAddOpen}>
            <DialogTrigger asChild>
              <Button onClick={() => setEditing(null)}>Thêm bàn</Button>
            </DialogTrigger>
            <DialogContent>
              <DialogTitle>Thêm bàn</DialogTitle>
              <TableForm areas={areas} onDone={() => { setAddOpen(false); refresh() }} />
            </DialogContent>
          </Dialog>
          <Button variant="outline" asChild>
            <Link to="/">Về trang chủ</Link>
          </Button>
        </div>
      </div>

      {error && <p className="text-sm text-destructive">{error}</p>}

      {/* Dialog sửa bàn */}
      <Dialog open={editOpen} onOpenChange={setEditOpen}>
        <DialogContent>
          <DialogTitle>Sửa bàn</DialogTitle>
          <TableForm
            areas={areas}
            editing={editing}
            defaultAreaId={editing?.area_id}
            onDone={() => { setEditOpen(false); refresh() }}
          />
        </DialogContent>
      </Dialog>

      <Tabs defaultValue="all">
        <TabsList>
          <TabsTrigger value="all">Tất cả</TabsTrigger>
          {areas.map((a) => (
            <TabsTrigger key={a.id} value={String(a.id)}>{a.name}</TabsTrigger>
          ))}
        </TabsList>

        <TabsContent value="all">
          <div className="mt-4 flex flex-wrap gap-3">
            {tablesForArea(null).map((s) => (
              <TableCard
                key={s.table.id}
                table={s.table}
                status={s.status}
                onEdit={onEdit}
                onDelete={onDelete}
                onToggleActive={onToggleActive}
              />
            ))}
            {tablesForArea(null).length === 0 && (
              <p className="text-muted-foreground">Chưa có bàn nào.</p>
            )}
          </div>
        </TabsContent>

        {areas.map((a) => (
          <TabsContent key={a.id} value={String(a.id)}>
            <div className="mt-4 flex flex-wrap gap-3">
              {tablesForArea(a.id).map((s) => (
                <TableCard
                  key={s.table.id}
                  table={s.table}
                  status={s.status}
                  onEdit={onEdit}
                  onDelete={onDelete}
                  onToggleActive={onToggleActive}
                />
              ))}
              {tablesForArea(a.id).length === 0 && (
                <p className="text-muted-foreground">Khu vực chưa có bàn.</p>
              )}
            </div>
          </TabsContent>
        ))}
      </Tabs>
    </div>
  )
}
```

- [ ] **Step 6: Thêm route + link** — trong `src/App.tsx` thêm `{ path: "/tables", element: <Tables /> }` (import `Tables from "@/routes/Tables"`); trong `src/routes/Home.tsx` thêm `Button asChild` link `to="/tables"` ("Sơ đồ bàn").

- [ ] **Step 7: Verify build** — Run: `npm run build` → OK.

- [ ] **Step 8: Commit**

```bash
git add src/routes/Tables.tsx src/routes/tables/ src/App.tsx src/routes/Home.tsx src/components/ui/
git commit -m "feat(tables): add /tables route with area tabs, table cards, area manager dialog"
```

---

## Task 8: Kiểm chứng end-to-end + cổng chất lượng

**Files:** none (verification).

- [ ] **Step 1: Chạy toàn bộ quality gate** —

```
npm run lint
npx prettier --check .
npm run test
cargo fmt --check --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
npm run build
```

Tất cả phải xanh. Sửa formatter/lint nếu cần (giữ logic, chỉ format).

- [ ] **Step 2: Kiểm chứng app thật** — Run: `npx tauri dev`. Thực hiện tuần tự:
  1. Mở "Quản lý khu vực" → thêm "Tầng 1" và "Tầng 2" → xác nhận hiện trong tab.
  2. Thêm bàn "B01" (Tầng 1, 4 ghế) và "B02" (Tầng 1, 2 ghế) → thấy card màu trạng thái "Trống".
  3. Thêm bàn "VIP-01" (Tầng 2, 6 ghế) → chuyển sang tab "Tầng 2" thấy đúng bàn.
  4. Nhấn "Tắt" trên B02 → card đổi sang "Ngừng dùng" (badge xám).
  5. Sửa tên VIP-01 → "VIP-A" → tên cập nhật.
  6. Xoá VIP-A.
  7. Thử xoá khu vực "Tầng 1" khi còn bàn → phải báo lỗi.
  8. Đóng app, mở lại → dữ liệu còn nguyên.
  9. Dừng `Ctrl+C`.

- [ ] **Step 3: Commit (nếu có sửa ở Step 1)** —

```bash
git add -A
git commit -m "chore(tables): fix lint/format and finalize M2 quality gates"
```

---

## Definition of Done (M2)

- [ ] Migration `0003_tables.sql` chạy sạch trên DB mới (fresh) và DB sau M1 (0001+0002 đã có); bảng `areas`, `tables` và index `idx_tables_area` tồn tại.
- [ ] CRUD khu vực: thêm/sửa/bật-tắt/xoá; xoá bị chặn khi còn bàn.
- [ ] CRUD bàn: thêm/sửa/bật-tắt/xoá; `seats` optional; `list_tables` lọc đúng theo `area_id` và `include_inactive`.
- [ ] `list_table_status` trả đúng `TRONG` + `open_order_id = null` cho tất cả bàn active (M2 stub; M3 sẽ override).
- [ ] Màn hình `/tables` hiển thị tab theo khu vực, card bàn với badge trạng thái, CRUD hoạt động, lỗi hiển thị rõ ràng.
- [ ] Rust in-memory tests cho `repo/tables` xanh (area CRUD, table CRUD, guard xoá khu vực, list_table_status M2).
- [ ] Toàn bộ quality gate xanh: `npm run lint`, `prettier --check`, `npm run test`, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test`, `npm run build`.
- [ ] PR M2 → CI xanh trên GitHub → merge (GitHub Flow).
